---
title: Tags
layout: layout
layout: page
---

<% blog.tags.each do |tag, pages| %>
  <h2 id="<%= h(tag) %>"><%= h(tag) %></h2>
    <ul>
    <% pages.each do |page| %>
      <li><%= link_to(h(page.data.title), page) %></li>
    <% end %>
    </ul>
  </li>
<% end %>
