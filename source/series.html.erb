---
title: Series
layout: layout
group: navigation
layout: page
---

<% series.each do |series, pages | %>
  <h2 id="<%= h(series) %>"><%= h(series) %></h2>
    <ul>
    <% pages.each do |page| %>
      <li><%= link_to(h(page.title), page) %></li>
    <% end %>
    </ul>
  </li>
<% end %>


