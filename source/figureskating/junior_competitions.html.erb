---
title: ジュニア、ローカル大会
category: figureskating
---

<%   now = Time.now %>
  
<h2>開催予定</h2>

<% data.figureskating["competitions"].select {|c| !passed?(c, now) }.each {|c| %>
  <%= showdata(c) %>
<% } %>
  

<h2>開催済み</h2>

<% data.figureskating["competitions"].select {|c| passed?(c, now) }.each {|c| %>
  <%= showdata(c) %>
<% } %>
  
